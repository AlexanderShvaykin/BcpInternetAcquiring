source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in bcp_internet_acquiring.gemspec
gemspec

group :develop do
  gem 'byebug'
  gem 'pry',     require: false
  gem 'rubocop', require: false
end